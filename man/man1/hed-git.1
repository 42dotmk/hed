'\" t
.\" Automatically generated by Pandoc 3.6
.\"
.TH "HED-GIT" "1" "2026-08-22" "hed v1.21.2" "hed manual"
.SH NAME
hed\-git \- Launches lazygit as a full\-screen TUI from inside hed.
The editor temporarily leaves raw mode (the same dance fzf uses), hands
the terminal over to lazygit, and repaints when it exits.
.PP
Launches \c
.UR https://github.com/jesseduffield/lazygit
lazygit
.UE \c
\ as a full\-screen TUI from inside hed.
The editor temporarily leaves raw mode (the same dance fzf uses), hands
the terminal over to lazygit, and repaints when it exits.
.SH Commands
.PP
.TS
tab(@);
l l.
T{
Command
T}@T{
Action
T}
_
T{
\f[CR]:git\f[R]
T}@T{
Run lazygit in the current working directory
T}
.TE
.SH Keybind
Bound to \f[CR]<space>gg\f[R] by default in \f[CR]src/config.c\f[R]:
.IP
.EX
cmapn(\[dq] gg\[dq], \[dq]git\[dq], \[dq]lazygit\[dq]);
.EE
.SH Requirements
\f[CR]lazygit\f[R] on \f[CR]$PATH\f[R].
If it isn\[cq]t installed, \f[CR]:git\f[R] reports
\f[CR]failed to run lazygit\f[R] and leaves the editor untouched.
.SH Disable
Set \f[CR]plugin_load(&plugin_git, \&...)\f[R] to \f[CR]0\f[R] in
\f[CR]src/config.c\f[R] and \f[CR]:reload\f[R].
