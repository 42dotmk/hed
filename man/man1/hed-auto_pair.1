'\" t
.\" Automatically generated by Pandoc 3.6
.\"
.TH "HED-AUTO_PAIR" "1" "2026-08-23" "hed v1.21.3" "hed manual"
.SH NAME
hed\-auto_pair \- Auto\-inserts the closing half of a pair when you type
the opening half \[em] (, [, {, \[lq], \[cq], \[em] and steps the cursor
over the close character if you\[cq]re already sitting on it.
.PP
Auto\-inserts the closing half of a pair when you type the opening half
\[em] \f[CR](\f[R], \f[CR][\f[R], \f[CR]{\f[R], \f[CR]\[dq]\f[R],
\f[CR]\[aq]\f[R], \f[CR]\[ga]\f[R] \[em] and steps the cursor over the
close character if you\[cq]re already sitting on it.
.SH Behavior
.PP
.TS
tab(@);
lw(35.0n) lw(35.0n).
T{
You type
T}@T{
Result
T}
_
T{
\f[CR](\f[R] \f[CR][\f[R] \f[CR]{\f[R]
T}@T{
Inserts pair, cursor between
T}
T{
\f[CR]\[dq]\f[R] \f[CR]\[aq]\f[R] \f[CR]\[ga]\f[R] (and you\[cq]re not
on a matching close)
T}@T{
Inserts pair, cursor between
T}
T{
\f[CR]\[dq]\f[R] \f[CR]\[aq]\f[R] \f[CR]\[ga]\f[R] (and the next char
already matches)
T}@T{
Cursor steps over the existing close
T}
T{
\f[CR]<BS>\f[R] on an opener whose close is to the right
T}@T{
Deletes both halves
T}
.TE
.PP
Active in INSERT mode only.
Policy\-free: no special handling for strings inside comments or escaped
characters \[em] if you don\[cq]t want a pair, just press Backspace.
.SH Disable
Set \f[CR]plugin_load(&plugin_auto_pair, \&...)\f[R] to \f[CR]0\f[R] in
\f[CR]src/config.c\f[R] and \f[CR]:reload\f[R].
